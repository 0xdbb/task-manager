# fly.toml app configuration file generated for task-manager-6x3jxg on 2025-04-03T07:43:25Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#
app = "task-manager-6x3jxg"
primary_region = "ams"

[build]
# Your build configuration remains the same

[http_service]
  internal_port = 8000  # API port
  force_https = true
  processes = ["api"]   # This names your API process

[processes]
  api = "api/main"      # Runs your API
  worker = "worker/main" # Runs your worker

[[vm]]
  memory = "1gb"
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 1024
